project_id = "hvkcdezablbqgahvumao"  # Replace with your project ref (e.g. "abcdefghijklmnop") — find it at supabase.com/dashboard → project settings → general

[db]
port = 5432
shadow_port = 5433
major_version = 15

[studio]
enabled = true
port = 54323

[storage]
enabled = true

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = []
jwt_expiry = 3600
enable_signup = true
